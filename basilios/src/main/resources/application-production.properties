# ===========================================
# PERFIL DE PRODUÇÃO - SEGURANÇA OWASP
# ===========================================

# Desabilitar exposição de erros internos
server.error.include-binding-errors=never
server.error.include-message=never
server.error.include-stacktrace=never

# Desabilitar H2 Console
spring.h2.console.enabled=false

# Conexão com SSL habilitado
spring.datasource.url=${DB_URL}

# Desabilitar SQL logging
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.use_sql_comments=false

# Logging seguro para produção
logging.level.com.basilios.basilios=WARN
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN
